import model.Board
import chess.api._
object History {
    
    def unmoved(board: Board, history: Seq[Action], pieceAt: (Int,Int)): Boolean = {
//        history find { x => x.id == piece.id } isDefined
         true
    }

}